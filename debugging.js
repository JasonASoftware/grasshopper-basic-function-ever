
//error bug default code
// function addFive(num) {
//   var total = num + 5
//   return num
// }

//debugged it by changing the variable its going to return from num to total
function addFive(num) {
  var total = num + 5
  return total
}
